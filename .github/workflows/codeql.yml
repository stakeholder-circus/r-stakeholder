name: codeql

on:
  push:
  pull_request:
  schedule:
    - cron: '19 3 * * 1'

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CodeQL has no first-class R analysis lane; deterministic R validation runs in ci."
